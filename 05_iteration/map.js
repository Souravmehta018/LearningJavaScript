
const map = new Map();

map.set('Nic', 'Nicholas');
map.set('In', 'India');
map.set('Me', 'Mexico');
map.set('Fr', 'France');
map.set('It', 'Italy');
map.set('In', 'India');

console.log(map);

// maps are always unique and are stored in order. The last India won't be added.
// maps are not iteratable so for in/of loops are not possible.

